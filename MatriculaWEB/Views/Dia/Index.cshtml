@model List<Dia>
<!DOCTYPE html>

<html>
<head>
    <title>Gerenciamento de dias</title>
</head>
<body>
    <h2>Gerenciamento de dias</h2>
    <a asp-action="Cadastrar" asp-controller="Dia"
       class="btn btn-primary espacamento-baixo">Cadastrar</a>
    <table class="table table-hover table-striped espacamento-baixo">
        <thead>
            <tr>
                <th>Id</th>
                <th>Descrição</th>
                <th>Ordenação</th>
                <th>Criado em</th>
                <th></th>
                <th></th>
            </tr>
        </thead>
        <tbody>
            @foreach (Dia diaCadastrado in Model)
            {
                <tr>
                    <td>@diaCadastrado.Id</td>
                    <td>@diaCadastrado.Descricao</td>
                    <td>@diaCadastrado.Ordenacao</td>
                    <td>@diaCadastrado.CriadoEm.ToString(@*"dd/MM/yyyy hh:mm:ss"*@)</td>
                    <td>
                        <a asp-action="Remover"
                           asp-controller="Dia"
                           asp-route-id="@diaCadastrado.Id"
                           class="btn btn-danger">Remover</a>
                    </td>
                    <td>
                        <a asp-action="Alterar"
                           asp-controller="Dia"
                           asp-route-id="@diaCadastrado.Id"
                           class="btn btn-warning">Alterar</a>
                    </td>
                </tr>
            }
        </tbody>
    </table>

    <span><b>Quantidade de registros: </b> @Model.Count </span><br />
    <span><b>Dados atualizados: </b> @DateTime.Now </span>

</body>
</html>